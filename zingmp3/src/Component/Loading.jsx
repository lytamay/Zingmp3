import React from "react";

const Loading = () => {
    let Style = {
        color: "blue",
        fontsize: "12px",
        display: "flex",
        aligeitem: "center",
        justifycontent: "center",
    };
    return (
        <div className="Loading" style={Style}>
            loading
        </div>
    );
};

export default Loading;
